<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Cambia el color de fondo de la ActionBar -->
        <item name="colorPrimary">@color/azul_claro</item>
        <!-- Cambia el color del texto y los íconos en la ActionBar -->
        <item name="colorPrimaryDark">#3c6cb5</item>
    </style>

    <style name="Botones" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/azul_claro</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>